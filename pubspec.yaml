name: flutter_vk_id_auth_demo
description: "Flutter VK ID Auth Demo."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  vkid_flutter_sdk: ^1.0.3
  bloc: ^9.1.0
  flutter_bloc: ^9.1.1
  flutter_secure_storage: ^10.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
